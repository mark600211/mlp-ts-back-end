import { CustomersResolver } from './customers.resolver';
import { GeneralCustomerResolver } from './general-customers.resolver';
import { LabResolver } from './labs.resolver';

export const Resolvers = [
  CustomersResolver,
  GeneralCustomerResolver,
  LabResolver,
];
