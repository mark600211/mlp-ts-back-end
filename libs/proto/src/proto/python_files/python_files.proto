syntax = "proto3";

package python_files_service;

service PythonFilesService {
    rpc DownloadFile (Path) returns (stream File);
    rpc UpdloadFile (Test) returns (Test);
}

message Test {
    string message = 1;
}

message Path {
    string path = 1;
}

message File {
    bytes chunk = 1;
}